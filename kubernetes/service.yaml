apiVersion: v1
kind: Service
metadata:
  name: sovereign-api
  labels: { app: sovereign }
spec:
  selector: { app: sovereign }
  ports:
    - { name: http, port: 8000, targetPort: 8000 }
  type: ClusterIP
