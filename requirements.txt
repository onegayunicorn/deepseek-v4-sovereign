# SOVEREIGN — runtime dependencies (Python 3.11+)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
typer>=0.12,<1.0
pydantic>=2.6
pyyaml>=6.0
httpx>=0.27
websockets>=12.0
cryptography>=42.0
openai>=1.30

# Optional / model-side
# torch>=2.2            # local inference / embeddings (heavy)
# transformers>=4.40    # Fish Audio S2-Pro + DeepSeek tokenizer
# safetensors>=0.4
# accelerate>=0.28
# huggingface_hub>=0.22
# chromadb>=0.4         # vector memory
# numpy>=1.26
